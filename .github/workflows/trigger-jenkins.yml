name: Trigger Jenkins

on:
  push:
    branches: main

jobs:
  trigger-jenkins:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Jenkins build
        run: |
          curl -X POST ip10-0-42-5-d34gventq0k1c7cormug-8080.direct.lab-boris.fr/job/serverless-demo/build --user "bstocker:11c7b774a7af4224d1baebb2d2b7905660"
